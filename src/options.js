import React from 'react';
import { launchOptions } from "./services/options"

const Options = () => {
    return <div className="footer">
        <a href="#" onClick={launchOptions}>options</a>
    </div>
}

export default Options